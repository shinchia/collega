<div class = "video_new">
  <div class ="edit_zenntai">
    <h4>ユーザー一覧</h4>
    <% @teachers.each do |teacher| %>

          <h5><%= link_to(teacher.name, "/teachers/#{teacher.id}") %></h5>

    <% end %>
  </div>
  </div>
